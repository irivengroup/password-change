---
- name: Converge changepassword negative scenario
  hosts: localhost
  connection: local
  gather_facts: true
  become: false

  vars:
    changepassword_use_no_log: false
    changepassword_audit_enabled: false
    changepassword_lock_enabled: false
    changepassword_check_shadow_backend: false
    changepassword_set_aging: false
    changepassword_require_etc_passwd_user: false
    changepassword_fail_if_same_hash: false
    changepassword_hmac_salt_secret: Molecule-Hmac-Secret-2026!
    changepassword_target_account: ghost
    changepassword_local_accounts:
      - username: root
        password: "$6$rounds=656000$abcdefghijklmnop$01234567890123456789012345678901234567890123456789012345678901234567890123456789"
        state: unlocked
        expire: false

  tasks:

    - name: Negative | Run role and capture expected preflight rejection
      block:

        - name: Negative | Include changepassword role with unknown target
          ansible.builtin.include_role:
            name: changepassword


        - name: Negative | Mark unexpected success
          ansible.builtin.set_fact:
            changepassword_negative_rejected: false

      rescue:

        - name: Negative | Mark expected failure as rejected
          ansible.builtin.set_fact:
            changepassword_negative_rejected: true


    - name: Negative | Assert unknown requested account is rejected
      ansible.builtin.assert:
        that:
          - changepassword_negative_rejected | default(false) | bool
        fail_msg: Unknown changepassword_target_account was not rejected.
