---
- name: Verify default Molecule scenario
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Verify | Assert root target remains configured in scenario
      ansible.builtin.assert:
        that:
          - changepassword_target_account | default('root') == 'root'
      vars:
        changepassword_target_account: root
