---
- name: Verify changepassword default contract
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:


    - name: Verify | Scenario contract placeholder
      ansible.builtin.assert:
        that:
          - true
